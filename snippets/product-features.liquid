{% assign product_obj = all_products[product] %}

{%- comment -%}
  Debug: Output raw metafield value (should look like an array)
{%- endcomment -%}
Raw features: {{ product_obj.selected_or_first_available_variant.metafields.amzdata.features }}

{%- comment -%}
  Use the metafield directly since it’s already an array.
{%- endcomment -%}
{% assign features = product_obj.selected_or_first_available_variant.metafields.amzdata.features %}

{% if features and features.size > 0 %}
  <ul>
    {% for feature in features %}
      <li>{{ feature }}</li>
    {% endfor %}
  </ul>
{% else %}
  <p>No features available.</p>
{% endif %}

<p>Test</p>
<p>{{ product_obj.title }}</p>
<p>wrytrwy</p>
